/**
 * Solve quick union
 * Explaareination:
 *  - Generate 0-n nodes.
 *  - Create @union method to join two nodes for example: 2, 5
 *          1        2
 *        / \        \
 *       6   3        5
 *  - Create @connected method. input: node1, node2 -> true if nodes are connected, false if not
 */
export class QuickUnion {}
