/**
 * Calculate word area:
 *   - calculate area of the word
 *   - area should be (max height of the letter in word) * (length of the word)
 *   - Example : ([1,2,3 ..... 26], 'abc') : 3 * 3 -> 9
 * @param heights Array of length 26 where 0 < n < 100,
 *                represents height of each letter corresponds to their index
 * @param word string of english alphabets where letter a -> z
 */
export const calcWordHeight = (heights, word) => {};
